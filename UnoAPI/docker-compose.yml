services:
  redis:
    image: redis:7-alpine
    container_name: uno-api-redis
    ports:
      - "6379:6379"
    volumes:
      - uno-api-redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  uno-api-redis-data:
